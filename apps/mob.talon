tag: terminal

-

mob start:
    insert("mob start")
mob next <user.number_string>:
    insert("mob next {number_string}")
    key(enter)
mob next:
    insert("mob next")
    key(enter)
mob done:                   "mob done"
mob timer open:             "mob timer open"
